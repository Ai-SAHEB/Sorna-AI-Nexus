name: Sorna AI Autonomous Evolution

on:
  schedule:
    - cron: '0 */6 * * *'  # هر 6 ساعت اجرا شود
  workflow_dispatch:  # اجرای دستی
  push:
    branches: [ main ]

jobs:
  evolve:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.SAHEB_AI_TOKEN }}
        
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Sorna AI Autonomous System
      env:
        SAHEB_AI_TOKEN: ${{ secrets.SAHEB_AI_TOKEN }}
      run: |
        echo "🚀 شروع سیستم خودمختار Sorna AI..."
        python sorna_ai.py
        
    - name: Commit and push changes
      run: |
        git config --local user.email "ai@saheb.com"
        git config --local user.name "Sorna AI Bot"
        git add .
        git commit -m "🤖 Autonomous evolution: $(date +'%Y-%m-%d %H:%M:%S')" || exit 0
        git push
